{
	"version": "1.0.0-*",

	"dependencies": {
		"Microsoft.NETCore.App": "1.0.1"
	},

	"frameworks": {
		"netcoreapp1.0": {
			"imports": [
				"netcore50",
				"portable-net45+win8"
			]
		}
	},
	"scripts": {
		"postcompile": [ "dotnet pack --no-build --configuration %compile:Configuration%" ]
	}
}
